type vendor_hal_performance_default, domain;
hal_server_domain(vendor_hal_performance_default, vendor_hal_performance)

type vendor_hal_performance_default_exec, exec_type, vendor_file_type, file_type;
init_daemon_domain(vendor_hal_performance_default)

binder_call(vendor_hal_performance_client, vendor_hal_performance_server)
binder_call(vendor_hal_performance_server, vendor_hal_performance_client)

hal_attribute_hwservice(vendor_hal_performance, vendor_hal_performance_hwservice)

allow vendor_hal_performance_default hal_camera_default:dir search;
allow vendor_hal_performance_default hal_fingerprint_default:dir search;

rw_dir_file(vendor_hal_performance_default, oplus_scheduler_sched_assist_proc)
